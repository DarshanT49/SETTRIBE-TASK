spring.application.name=backend


# --- Server -------------------------------------------------
server.port=8080
server.address=0.0.0.0

# ============================================================
#  DATABASE - LOCAL (MySQL)
#  Active when running on your machine
# ============================================================
# spring.datasource.url=jdbc:mysql://localhost:3306/mtms_db?createDatabaseIfNotExist=true&useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
# spring.datasource.username=root
# spring.datasource.password=Prathm@2362
# spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
# spring.jpa.hibernate.ddl-auto=update
# spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect

# ============================================================
#  DATABASE - PRODUCTION (PostgreSQL / Neon)
#  Uncomment this block and comment the LOCAL block above before deploying
# ============================================================Api 
spring.datasource.url=jdbc:postgresql://ep-morning-surf-ape09tjl-pooler.c-7.us-east-1.aws.neon.tech/neondb?user=neondb_owner&password=npg_xbBp9yctouP7&sslmode=require&channelBinding=require
spring.datasource.username=neondb_owner
spring.datasource.password=npg_xbBp9yctouP7
spring.datasource.driver-class-name=org.postgresql.Driver
spring.jpa.hibernate.ddl-auto=update
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.datasource.hikari.connection-timeout=30000
spring.datasource.hikari.maximum-pool-size=5

# --- JPA (shared) -------------------------------------------
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=false

# --- JWT ----------------------------------------------------
# LOCAL  : hardcoded dev secret below is fine
# PROD   : replace with ${JWT_SECRET} and set in Render env vars
jwt.secret=super_secret_default_key_for_dev_only_change_in_prod_with_at_least_32_characters
#jwt.secret=${JWT_SECRET}
jwt.expiration=86400000

# LOCAL  : localhost origins below
# frontend.urls=http://localhost:5173

# PROD   : replace with your Render frontend URL
frontend.urls=https://settribe-task-1.onrender.com

# LiveKit
# LOCAL  : local LiveKit server
# livekit.url=ws://localhost:7880
# livekit.api-key=devkey
# livekit.api-secret=super_secret_livekit_key_that_is_at_least_32_chars_long


# PROD   : replace with LiveKit Cloud URL and credentials

livekit.url=wss://meeting-task-management-n5vtqjvl.livekit.cloud
livekit.api-key=APIScwRhqbXnCpV
livekit.api-secret=MSchtN3bfTeOfcrop7XX2OTqO1WqnE2stJObfojZLMiD

# Mail
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=loharshubham705@gmail.com
spring.mail.password=tmtp hopi rsxs szyi
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
